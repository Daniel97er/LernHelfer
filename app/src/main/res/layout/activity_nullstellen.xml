<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    android:background="#FFFFFF">

    <TextView
        android:id="@+id/textViewTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Nullstellen Berechnen"
        android:textSize="24sp"
        android:textColor="#000000"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="24dp"
        android:layout_marginTop="24dp"
        android:textStyle="bold" />

    <EditText
        android:id="@+id/editFunction"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Funktion eingeben (z.B. 2x^4 - 4x^3 + 2x^2 - 4x + 2)"
        android:inputType="text"
        android:layout_below="@id/textViewTitle"
        android:layout_marginBottom="16dp"
        android:padding="12dp"
        android:background="#ff333333" />

    <Button
        android:id="@+id/buttonBerechnen"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Berechnen"
        android:background="#ff333333"
        android:textColor="#FFFFFF"
        android:textStyle="bold"
        android:layout_below="@id/editFunction"
        android:layout_marginBottom="24dp"
        android:padding="12dp" />

    <TextView
        android:id="@+id/textAusgabe"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="18sp"
        android:textColor="#000000"
        android:layout_below="@id/buttonBerechnen"
        android:padding="12dp"
        android:background="#ff333333" />
</RelativeLayout>